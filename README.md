# COMP-353-Assignments
## For 1.1:
The `1.1python_code.py` file contains the actual code that reads the three .txt files
The output is found on line 63 of the `1.1python_code.py` (commented out), or after the `print(run_query_for_json())` function call

## For 1.2:
The schema for creating and populating the tables is in `1.2schema.sql`, while the query is written in `1.2query.sql`, along the result
